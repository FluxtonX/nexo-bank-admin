const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); const env = fs.readFileSync('.env.local', 'utf8'); const urlMatch = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/); const keyMatch = env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/); const supabase = createClient(urlMatch[1].trim(), keyMatch[1].trim()); async function check() { const { data: w } = await supabase.from('withdrawal_requests').select('*').limit(1); console.log('Withdrawals:', w); const { data: d } = await supabase.from('deposit_requests').select('*').limit(1); console.log('Deposits:', d); } check();
